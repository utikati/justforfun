function ImageSaint() {
  const st = {
    backgroundColor: "rgb(220, 220, 220)",
  };
  const img = {
    borderRadius: "15px",
  };
  return (
    <div style={st}>
      <img src="/sabio.png" style={img}></img>
    </div>
  );
}

export default ImageSaint;
